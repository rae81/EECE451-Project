# EECE451-Project

Backend work for the EECE 451 Network Cell Analyzer project now lives in [`server/`](./server).

The current backend includes:
- Flask application scaffold
- SQLAlchemy models for uploaded cell data and device activity
- JSON API endpoints for ingest and statistics
- HTML dashboard pages for central and per-device views
- Local SQLite development setup and deployment files for Render
