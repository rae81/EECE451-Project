================================================================================
 Dead-Zone Model Figures (extracted from server/deadzonePM.ipynb)
 Every figure here was generated by the notebook cells during its last full
 execution. Include them in the report as-is; filenames are already ordered
 by section so they flow with the notebook narrative.
================================================================================

01_2a_geographic_coverage_speed_distribution.png
    Section 2a. EDA — geographic coverage of every input dataset plus speed
    distribution histograms. Sanity-check of geographic bounds and sample
    density per region.

02_2b_speed_vs_latency_deadzone_quadrant.png
    Section 2b. Dead-zone identification — speed × latency scatter. The
    red quadrant (<1 Mbps AND >100 ms) matches ITU-T Y.1541's
    "unacceptable for interactive traffic" threshold.

03_3a_dataset_fusion_training_build.png
    Section 3a. Dataset fusion — training set built from the P.1812 physics
    backend plus topology features; shows how the fused dataset is
    assembled across tiers.

04_3c_label_quality_class_balance.png
    Section 3c. Label-quality visualisation — class balance per tier,
    per-tier confidence weighting, and geographic distribution of dead
    zones across all three tiers.

05_4c_weak_supervision_relabeling_before.png
    Section 4c. Topology-tier rows BEFORE weak-supervision relabeling.

06_4c_weak_supervision_relabeling_after.png
    Section 4c. Topology-tier rows AFTER weak-supervision relabeling.
    Shows how the single-source P.1812 labels become more robust under
    multi-signal aggregation.

07_4d_feature_correlation_with_deadzone_label.png
    Section 4d. Per-feature Pearson correlation between each numeric
    feature and the fused binary dead-zone label.

08_6a_metrics_confusion_matrix.png
    Section 6a. Evaluation — confusion matrix and metrics on the training
    set plus spatial cross-validation reference.

09_6b_prediction_distribution_diagnostics.png
    Section 6b. Prediction-distribution diagnostics — actual vs predicted
    RSRP on OOF samples (regressor calibration) and classifier probability
    distribution by true label.

10_6c_berlin_v2x_transfer.png
    Section 6c. External geographic transfer — Berlin V2X dataset
    evaluation. Runs only when the V2X data files are present on disk.

11_6c2_ookla_berlin_crosscity_transfer.png
    Section 6c2. Cross-city transfer — Ookla Open-Data Berlin tiles scored
    by the Lebanon-trained model. Always runs.

12_7a_shap_global_feature_importance.png
    Section 7a. SHAP explainability — global feature importance bar
    (mean |SHAP|). Per-prediction attribution produced via TreeExplainer.

================================================================================
 To regenerate these figures: open server/deadzonePM.ipynb and run all cells.
 The notebook pins a fixed random seed, so re-runs reproduce identical plots.
================================================================================
