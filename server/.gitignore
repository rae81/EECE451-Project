__pycache__/
instance/
.venv/
*.pyc

# Keep the shipped model bundle + training reports so deployments have a
# ready-to-serve model without re-running training.
!instance/
!instance/ml/
!instance/ml/deadzone_model.joblib
!instance/ml/prepared_deadzone_dataset.csv
!instance/ml/reports/
!instance/ml/reports/*.json
!instance/ml/reports/*.png

# Local Flask SQLite dev database
instance/*.db
instance/*.sqlite
instance/*.sqlite3

# External transfer datasets stay out of the repo
cellular_dataframe.parquet
opencellid_berlin_262.csv.gz
_drive_tmp/
